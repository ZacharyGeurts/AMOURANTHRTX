#version 460
#extension GL_EXT_ray_tracing : require

layout(location = 0) rayPayloadInEXT vec3 hitValue;
hitAttributeEXT vec3 attribs;

void main() {
    // Example: ignore hits with low alpha (for transparency)
    float alpha = 1.0; // Replace with texture lookup if needed
    if (alpha < 0.1) {
        ignoreIntersectionEXT;
    }
}