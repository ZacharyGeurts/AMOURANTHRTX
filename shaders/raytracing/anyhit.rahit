#version 460
#extension GL_EXT_ray_tracing : require

layout(location = 0) rayPayloadEXT vec3 hitValue;

void main() {
    // Simple alpha test or ignore (extend with texture lookup if needed)
    if (true) {  // placeholder alpha test
        ignoreIntersectionEXT;
    }
}