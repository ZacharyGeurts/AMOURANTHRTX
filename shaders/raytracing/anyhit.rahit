#version 460
#extension GL_EXT_ray_tracing : require

layout(location = 1) rayPayloadEXT bool shadowHit;

void main() {
    // For now — just accept all hits (no alpha test)
    // Later: if (texture alpha < 0.5) ignoreIntersectionEXT();
}