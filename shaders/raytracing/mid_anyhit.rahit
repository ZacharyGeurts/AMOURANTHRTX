#version 460
#extension GL_EXT_ray_tracing : require

struct RayPayload {
    vec4 color;
};

layout(location = 0) rayPayloadInEXT RayPayload payload;

void main() {
    const float dist = gl_HitTEXT;
    const float atten = exp(-0.1 * dist);
    payload.color.rgb *= atten;
}